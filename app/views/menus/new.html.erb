<div class='menu-new-mian'>
 <div class='menu-new-text'>
   献立をたてる
 </div>

 <div class='menu-new-calender'>
   <div class='menu-new-date'>
     <%= "#{@selected_date.strftime('%m月%d日')}分の献立を作成"%> 
   </div>
   <div class='new-menus'>
     <%= form_tag(new_menu_for_date_path(@selected_date), method: :get, class: 'menu-form') do %>
     <%= select_tag(:category_id, options_for_select(Category.all.map { |c| [c.name, c.id] }), prompt: 'カテゴリーを選択') %>
     <%= text_field_tag(:search_term, params[:search_term], placeholder: 'レシピ名または材料で検索') %>
     <%= submit_tag '検索' %>
    <% if @recipes.present? %>
    <% @recipes.each do |recipe| %>
       <p><%= recipe.title %></p>
       <%end%>
       <% else %>
       検索結果はありません
    <% end %>
   <%end%> 

  </div>    
