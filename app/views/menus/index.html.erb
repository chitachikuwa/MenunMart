<%= render "shared/header" %>

<div class='manu-main'>  
  <div class='calender-text'>
    献立カレンダー
  </div>
  <div class='manu-text'>
    献立を作成したい日付をクリックしてください
  </div>  
  
  <div class='calender'> 
   <%= month_calendar events: @menus do |date,all| %>
     <%= link_to new_menu_for_date_path(date), class: 'crender-date-menu' do %>
   <%= date.day %>
   <%= all.each do |menu| %>
  <%end%> 
<% end %>
<% end %>
</div>
</div>



<%= render  "shared/footer" %>